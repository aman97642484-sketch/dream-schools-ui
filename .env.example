# Frontend env — optional.
# Leave unset to use the built-in same-origin /api/public routes (works in preview).
# Set to your Express backend URL (including /api) to use the Node backend instead.
# VITE_API_URL=http://localhost:5000/api
